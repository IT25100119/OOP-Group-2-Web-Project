spring.application.name=BikeRentalPlatform
server.port=8080

# JSP View Resolver
spring.mvc.view.prefix=/WEB-INF/views/
spring.mvc.view.suffix=.jsp

# Data storage path (relative to project root)
app.data.path=data/

# Session timeout
server.servlet.session.timeout=30m
